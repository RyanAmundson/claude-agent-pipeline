# Glossary

Project-specific terms used by this fixture. Scanner and other agents consult this before coining new terms.

- **fixture**: a synthetic project used by the agent-pipeline E2E test suite
- **finding**: an issue identified by the scanner agent (silent error, dead code, etc.)
- **ticket**: a JSON file under `.pipeline/queue/<state>/<id>.json` representing one unit of work
